//param_obj_type //figure out how to get arguments

//if place_meeting(x, y+1, param_obj_type) and place_meeting(x, y-1, param_obj_type) and place_meeting(x-1, y, param_obj_type) and place_meeting(x+1, y, param_obj_type) {
    //tile_add(
    //use_tile(2, 2) //this is a made up function, figure out what the actual one is called
//}

//place_meeting(x, y, param_obj_type)
